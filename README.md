# ABM for Exclusion in the Dating Market


### Installation
To install the necessary dependencies, run the following command:

```uv sync```

### Usage
To run the simulation, execute the following command:

```uv run main.py```
